services:
  - type: web
    name: hotel-system-backend
    runtime: java
    plan: free

    buildCommand: ./mvnw clean package -DskipTests

    startCommand: java -jar target/system-0.0.1-SNAPSHOT.jar

    envVars:
      - key: JAVA_VERSION
        value: 21